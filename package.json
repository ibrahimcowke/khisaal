{
  "name": "khisaal-encyclopedia-reader",
  "version": "1.0.0",
  "private": true,
  "description": "موسوعة الخصال المائتان وإمتاع القارئ في الأدب العربي",
  "scripts": {
    "dev": "npm --prefix reader-app run dev",
    "build": "npm --prefix reader-app run build",
    "preview": "npm --prefix reader-app run preview"
  }
}
